// import $ from 'jquery';
// import EmojiMenuInModal from '../../../app/assets/javascripts/set_status_modal/emoji_menu_in_modal';

describe('EmojiMenuInModal', () => {
  describe('postEmoji', () => {
    it('updates the status emoji when a new emoji is selected', () => {

    });

    it('calls `selectEmojiCallback` when the emoji is posted', () => {

    });
  });
});
